name: test

on:
  pull_request:
  push:
    branches: [llteacher01]

jobs:
  web:
    name: apps/web — typecheck, migrate, test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: llteacher
          POSTGRES_PASSWORD: dev
          POSTGRES_DB: llteacher
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U llteacher -d llteacher"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgres://llteacher:dev@localhost:5432/llteacher
      # Also read by the "Test" step below (scripts/seed.test.ts calls the
      # seed script itself) -- not just the smoke-test step -- so these live
      # at job level like DATABASE_URL, not scoped to one step.
      ENCRYPTION_KEY: ${{ secrets.CI_SEED_ENCRYPTION_KEY }}
      BLIND_INDEX_KEY: ${{ secrets.CI_SEED_BLIND_INDEX_KEY }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install psql
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends postgresql-client

      - name: Apply Postgres extensions
        run: psql "$DATABASE_URL" -f apps/web/src/db/init/01_extensions.sql

      - name: Apply Drizzle migrations
        working-directory: apps/web
        run: npm run db:migrate

      - name: Seed smoke test
        working-directory: apps/web
        run: npm run db:seed -- --reset

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test
