{
  "name": "infra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run && node --test scripts/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "pulumi:local": "npm run build && PULUMI_BACKEND_URL=file://$PWD/.pulumi/local pulumi --stack local",
    "pulumi:production": "npm run build && PULUMI_BACKEND_URL=s3://llteacher-pulumi-state-055237683908-us-west-2/llteacher-infra AWS_REGION=us-west-2 pulumi --stack production"
  },
  "dependencies": {
    "@pulumi/aws": "7.46.0",
    "@pulumi/pulumi": "3.262.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.0"
  }
}
